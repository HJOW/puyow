## 언젠간 수정해야 할 사항
   

온라인 플레이 구현
온라인 플레이 - 계정 시스템, 로그인, 대기실 구현

   메인 메뉴에서 "너랑 나랑" 클릭 시 나오는 선택지에서
   "온라인 플레이" 선택 시
   온라인 플레이 로그인 화면으로 넘어가야 함.
   ("온라인 플레이" 사용가능 API 또한 { "available" : true } 리턴하도록 수정되어야 함 .)
   
   계정 시스템을 구현해야 함.
   설정 화면에서 입력한 이름은 온라인 플레이에서는 아예 사용하지 않을 예정.
   서버가 구동되는 시스템에서
       [홈디렉토리]/.puyowserver/account/ 
   위 디렉토리 내에, 사용자 ID값을 이름으로 한 디렉토리가 있고, 그 안의 account.json 파일에 저장됨. (여기서 [홈디렉토리] 는 OS 사용자 홈 폴더를 의미)
   사용자 계정 정보 JSON 에는, 사용자 ID, 비밀번호, 닉네임, WIN POINT 점수 (차후 이용할 예정, 0 이상의 정수 값) 가 들어감.
   비밀번호의 경우 단방향 암호화를 sha256으로 1회 bcrypt 로 1회 총 2회 수행함.
   프론트에서 비밀번호 입력하여 서버에 전송 시 단방향 암호화를 sha256 으로 1회만 하여 전송, 서버에서 bcrypt 로 1회 더 암호화하여 비교하거나 저장.
   (백엔드에서 단방향 암호화 구현 시 npm install 필요한 경우 package.json 의 dependencies 에 추가, pip install 필요한 경우 python/pythonserver.py 상단의 주석에 관련 내용 반영. 프론트의 경우 모던 브라우저들을 위한 Web Crpyto API 사용.)
   (node.js 백엔드와 python 백엔드 둘 다 호환되어야 함. 그외 백엔드나 Firebase 는 고려하지 않음)

   온라인 플레이 로그인 화면에는, ID 와 비밀번호 입력 란과 "로그인", "가입", "취소" 버튼이 있어야 함.
   (로그인 화면의 비밀번호 입력칸도 회원가입 화면과 같이 마스킹 처리)
   각 입력칸 및 버튼 모두 방향키로 포커스 및 엔터 키로 선택, 또는 마우스로 클릭이 가능해야 함. (설정 화면 참고)

   "가입" 클릭 시 회원가입 화면으로 이동.
   회원가입 화면에는, ID와 닉네임, 비밀번호 입력칸과, "가입", "취소" 버튼이 있어야 함.
   (비밀번호 입력칸은 마스킹 처리)
   각 입력칸 및 버튼 모두 방향키로 포커스 및 엔터 키로 선택, 또는 마우스로 클릭이 가능해야 함. (설정 화면 참고)
   "가입" 버튼 클릭 시, 유효성 검사 후 실제로 가입 처리하고 "온라인 플레이 로그인 화면"으로 돌아가아 함.
   유효성 검사 시, 
       ID 는 고유해야 하고, 알파벳과 숫자 및 언더바만 이용 가능, 글자 수 4자리 이상 20자리 이하.
       ID 중복검사 시 대소문자를 가리지 않아야 함.
       비밀번호는 알파벳과 숫자, 언더바 및 일부 특수문자 (!, @, #, $, %, ^, &, *, ?) 만 이용 가능, 글자 수 4자리 이상 30자리 이하.
       닉네임은 알파벳과 숫자, 언더바만 이용 가능. 닉네임도 고유해야 함. 단 닉네임은 ID와는 달리 대소문자를 가림. 닉네임은 글자 수 3자리 이상 20자리 이하.
       

   비밀번호 틀려 로그인 실패 발생 시, 서버는 이를 메모리 상에 5분간 가지고 있어야 함.
   동일 계정 비밀번호 5분 내 3회 이상 틀리면 이 5분 동안 이 계정으로 로그인 불가. (이 5분 동안은 올바른 비밀번호로도 로그인 불가.)

   계정으로 로그인 시, 이미 해당 계정이 로그인된 상태라면
   기존 로그인된 세션을 무효화해야 함. 
   이 경우 발생 시 기존 로그인 세션에서는, 대기실인 경우 즉시 메시지 호출 및 로그인 화면으로 나가져야 하고, 게임 중인 경우 양측 모두 메시지 출력하고 게임 중단 후, 상대방은 대기실로, 무효화된 세션의 경우 로그인 화면으로 나가져야 함. (이 때는 WIN POINT 변화 없음. WIN POINT 값은 차후 설명.)

   온라인 플레이 로그인 화면에서 로그인 시 "온라인 플레이 대기실 화면"으로 이동함.
   이 화면 상단에, "나가기", "방 생성" 버튼 및 현재 로그인한 본인 계정 닉네임 정보가 있고
   그 아래에 방 목록들이 아래로 쭉 나열되어야 함. (한 자리가 남은 방만 출력)
   각 버튼 및 목록 내 항목 모두 방향키로 포커스 및 엔터 키로 선택, 또는 마우스로 클릭이 가능해야 함. (설정 화면 참고)
   
   각 방에는 방 제목은 없고, 방 생성자 닉네임과 룰, 색상 수만 적혀있음.
   (어짜피 1:1 대결이므로 참여자 수를 표시할 필요가 없음.)
   방의 고유ID는 방 생성자 (방장) ID를 사용하면 되나, 방장이 나감으로써 방장 권한이 참여자에게 옮겨가는 경우 방의 고유ID가 참여자의 ID값으로 바뀌어야 함에 유의.

   서버는 "방" 정보를
       [홈디렉토리]/.puyowserver/rooms/
   위 디렉토리 안에 json 형식으로 저장해 관리.

   "온라인 플레이 대기실 화면" 에서 "나가기" 버튼 클릭 시 서버에 로그아웃 했음을 보낸 후 메인 메뉴로 돌아가면 됨.
   "온라인 플레이 대기실 화면" 에서 "방 생성" 버튼 클릭 시, 레이어 팝업으로 "색상 수"와 룰 ("기본 룰", "피버 룰", "피버 룰 (시작)") 선택지와 "생성", "취소" 버튼이 나타남.
   각 버튼 및 목록 내 항목 모두 방향키로 포커스 및 엔터 키로 선택, 또는 마우스로 클릭이 가능해야 함. (설정 화면 참고)
   "취소" 버튼 클릭 시 "온라인 플레이 대기실 화면" 으로 돌아감.
   "생성" 버튼 클릭 시 방 생성 및 바로 방에 입장함. (온라인 플레이 방 화면에 진입, 방장 권한을 가짐.)

   "온라인 플레이 대기실 화면" 의 방 목록에서 방을 클릭/선택 시 바로 방에 입장함. (온라인 플레이 방 화면에 진입, 참여자 권한을 가짐.)
   온라인 플레이 방 화면에서는 상단에 "나가기" 버튼 및 현재 로그인한 본인 계정 닉네임 정보가 있고 (이미 방 안에 있으므로 "방 생성" 버튼은 두지 않으며, 그 외 배치는 대기실 화면과 동일)
   화면 중앙 좌측에는 본인 정보 (닉네임 및 WIN POINT 점수), 우측에는 상대방 정보 (닉네임 및 WIN POINT 점수) 가 나타나야 함.
   (여기서 "나가기" 버튼 클릭 시 "온라인 플레이 대기실 화면"으로 이동. 본인이 본인이 방장인 상태에서 나간 경우, 참여자가 있으면 참여자에게 방장 권한이 넘어가야 하고, 참여자도 없으면 방이 없어져야 함.)
   본인이 방장인 경우, 화면 하단에 "시작" 버튼이 추가로 나타남.
   각 버튼 및 목록 내 항목 모두 방향키로 포커스 및 엔터 키로 선택, 또는 마우스로 클릭이 가능해야 함. (설정 화면 참고)

   온라인 플레이 방 화면에서 방장이 "시작" 버튼 클릭 시, 화면 음영처리 후 3초 딜레이, 이후 양측 (방장, 참여자) 모두 게임 화면으로 넘어가야 함.
   이 3초 도중 방장 혹은 상대방이 나가면 (네트워크 이슈로 상대방이 나갔다는 정보가 지연되어 도착하는 경우를 대비) 음영처리 다시 해제. (물론 이 경우 게임 화면으로 넘어가지 않음.)
   게임 화면으로 넘어가면 3초 카운트다운 후 게임이 시작함.
   
   서버는 양측 모두와 WebSocket 방식으로 통신하여 게임 진행하며, 
   대부분의 판정 및 뿌요 지급 큐 또한 서버가 결정해야 함.
   

   
   온라인 플레이 게임 시에는 ESC로도 일시정지가 불가능함.

   상대방이 연결이 끊긴 경우, 서버는 남은 사람에게 연결 끊김 상태를 알림
   이 알림을 받은 경우 메시지 출력 후 "온라인 플레이 대기실 화면" 으로 이동. (승패 처리 안하고 WIN POINT 점수도 변동 없음. 일단 고의적인 연결 끊김은 고려하지 않음.)

   게임이 정상적으로 종료 (한 쪽의 패배) 된 경우
   양측의 WIN POINT 가 변경됨.
   승리한 쪽의 WIN POINT 계산식은 다음과 같이 조정

       [WIN POINT 수정값] = floor( 3 + ( [나와 상대방의 WIN POINT 차이값] / 100 ) )
   
   여기서 [나와 상대방의 WIN POINT 차이값] 값은, 게임 전 "상대방의 WIN POINT - 나의 WIN POINT" 으로 계산
   단 [나와 상대방의 WIN POINT 차이값] 값은 최소값 0을 적용

   패배한 쪽의 WIN POINT 계산식은 다음과 같이 조정

       [WIN POINT 수정값] = floor( ( 1 + ( [상대방과 나의 WIN POINT 차이값] / 300 ) ) * (-1) )
   
   여기서 [상대방과 나의 WIN POINT 차이값] 값은, 게임 전 "나의 WIN POINT - 상대방의 WIN POINT" 으로 계산
   단 [상대방과 나의 WIN POINT 차이값] 값은 최소값 0을 적용

   결론적으로, 본인 승리 시, 상대방의 WIN POINT 가 나보다 더 높은 경우 본인은 WIN POINT 를 더 많이 받아야 하고, 그렇지 않은 경우 WIN POINT 를 덜 받아야 함.
               본인 패배 시, 상대방의 WIN POINT 가 나보다 더 높은 경우 본인은 WIN POINT 를 더 적게 잃어야 하고, 그렇지 않은 경우 WIN POINT 를 더 잃어야 함.
               기본적으로 양측이 WIN POINT 가 같은 상태인 상태에서 게임한 경우, 승리한 쪽이 얻는 점수는 패배한 쪽이 잃는 점수의 3배가 됨.

   계산 후 WIN POINT 값이 0 미만인 경우 0으로 다시 설정해야 함. (0 이상 정수값만 허용)

### 세부 결정 사항 (보편적인 온라인 대전 게임 방식 기준)

   위 요구사항 중 명시되지 않아 구현 전에 정해야 하는 부분을 아래와 같이 정한다.
   위에 이미 적힌 내용과 충돌해 어쩔 수 없이 바꾼 부분은 이 문서 최하단 "변경 사유"에 따로 적는다.

#### 1. 통신 방식과 API 경로

   - 로그인·가입처럼 게임 밖에서 한 번씩 일어나는 요청은 HTTP POST(JSON)로 처리한다. 경로는 기존 `/apis/...` 관례를 따라
     `/apis/onlineplay/signup`, `/apis/onlineplay/login`, `/apis/onlineplay/logout` 을 사용한다.
   - 로그인 성공 이후의 대기실·방·게임 진행은 WebSocket 연결 하나(`/apis/onlineplay/socket`)를 계속 유지하며 처리한다.
     대기실 방 목록도 클라이언트가 주기적으로 다시 묻지 않고, 목록이 바뀔 때 서버가 밀어 준다. (폴링하지 않는다.)
   - 모든 WebSocket 메시지는 UTF-8 JSON 한 개이며 `{ "type": "...", ... }` 형태다.
     오류는 `{ "type": "error", "code": "...", "message": "..." }` 로 보내고, 클라이언트는 `message` 를 그대로 쓰지 않고 `code` 로 화면 문구를 고른다.
   - WebSocket 은 추가 의존성 없이 RFC 6455 최소 구현(핸드셰이크와 텍스트·핑·퐁·클로즈 프레임)을 `onlineplay.js`·`onlineplay.py` 안에 직접 만든다.
     node.js 쪽은 기존 `http` 서버의 `upgrade` 이벤트를, python 쪽은 `ThreadingHTTPServer` 핸들러에서 얻은 소켓을 사용한다.
     (python 서버에 WebSocket 라이브러리 설치를 요구하지 않기 위함이다. 단방향 암호화용 `bcrypt` 는 node.js 는 이미 `dependencies` 에 있고, python 은 `pip install bcrypt` 안내를 `python/pythonserver.py` 상단 주석에 추가한다.)

#### 2. 세션과 인증

   - 로그인 성공 시 서버는 128비트 이상 난수를 hex 문자열로 만든 세션 토큰을 발급하고 메모리에만 보관한다. 계정 파일에는 저장하지 않는다.
   - 클라이언트도 토큰을 메모리에만 두고 `puyow_store` 등 로컬 저장소에 저장하지 않는다. 페이지를 새로 고치면 다시 로그인한다.
     비밀번호 저장, 자동 로그인, ID 기억 기능은 이번 범위에 넣지 않는다.
   - WebSocket 연결 직후 첫 메시지로 `{ "type": "auth", "token": ... }` 를 보낸다. 5초 안에 유효한 토큰이 오지 않으면 서버가 연결을 끊는다.
   - 같은 계정으로 다시 로그인해 기존 세션을 무효화할 때는, 기존 토큰을 즉시 폐기하고 그 토큰으로 열려 있던 WebSocket 에
     `{ "type": "session_closed" }` 를 보낸 뒤 끊는다. 화면 이동은 위에 이미 명시한 규칙을 따른다.
   - 세션은 WebSocket 이 끊긴 뒤 60초 안에 같은 토큰으로 다시 연결되지 않으면 만료한다.
     대기실·방에서 30분 동안 아무 조작이 없으면 서버가 세션을 만료시키고 연결을 끊는다.
   - 서버는 5초마다 ping 을 보내고, 15초 동안 pong 도 다른 메시지도 오지 않으면 그 연결을 끊긴 것으로 판정한다.
     위에 적힌 "상대방이 연결이 끊긴 경우" 판정도 이 기준을 사용한다.
   - 평문 HTTP 구간에서는 sha256 해시 자체가 그대로 재사용될 수 있다. 공개망 운영은 HTTPS(WSS) 사용을 전제로 하며, 서버가 이를 강제하지는 않는다.

#### 3. 계정 저장 형식

   - `account.json` 은 `{ "id", "nickname", "password", "winPoint", "createdAt" }` 을 담는다.
     `id` 는 가입할 때 입력한 원본 표기, `password` 는 "프론트 sha256 → 서버 bcrypt" 를 거친 문자열,
     `winPoint` 는 0 이상 정수이고 가입 시 0, `createdAt` 은 ISO 8601 문자열이다.
   - 계정 디렉터리 이름은 ID 를 소문자로 바꾼 값을 쓴다. 중복 검사와 로그인 조회도 소문자로 맞춰 비교하므로,
     대소문자를 가리지 않는 규칙이 파일시스템 종류와 무관하게 같게 동작한다.
   - 닉네임은 대소문자를 가리므로 디렉터리 이름으로 쓸 수 없다. 서버는 시작할 때 계정 디렉터리를 한 번 읽어 닉네임→ID 색인을 메모리에 만들고, 가입할 때마다 갱신한다.
   - 유효성 검사는 프론트에서 했더라도 서버에서 반드시 다시 한다. 실패 시 사유(형식·길이·중복)를 오류 코드로 구분해 돌려준다.
   - 비밀번호 틀림 기록은 메모리에만 두므로 서버를 다시 켜면 사라진다. 잠긴 계정은 올바른 비밀번호로 시도해도 남은 잠금 시간을 알리는 오류 코드를 돌려준다.
   - 닉네임 변경, 비밀번호 변경, 계정 삭제는 이번 범위에 넣지 않는다.

#### 4. 대기실과 방

   - 방 파일은 `[홈디렉토리]/.puyowserver/rooms/[방ID].json` 이며, 정본은 서버 메모리이고 파일은 상태가 바뀔 때마다 덮어쓰는 스냅샷이다.
     접속자가 없는 방은 존재할 수 없으므로, 서버를 시작할 때 이 디렉터리를 비워 이전 실행의 방이 남지 않게 한다.
   - 방 JSON 은 `{ "id", "rule", "colorCount", "host", "guest", "state", "createdAt" }` 을 담는다.
     `host`·`guest` 는 각각 `{ "id", "nickname", "winPoint" }` 이고 참여자가 없으면 `guest` 는 null 이다. `state` 는 `waiting` 또는 `playing` 이다.
   - 방장 권한이 참여자에게 넘어가 방 고유ID가 바뀌면, 기존 파일을 지우고 새 ID 이름으로 다시 쓴다.
   - 한 계정은 동시에 한 방에만 들어갈 수 있다. 방에 있는 동안에는 다른 방에 들어가거나 새 방을 만들 수 없다.
   - 대기실 목록에는 `state` 가 `waiting` 이고 참여자가 없는 방만 담는다. 게임 중인 방은 목록에서 빠진다.
     정렬은 만든 시각 오름차순이며 한 번에 최대 50개를 보낸다. 서버가 유지하는 방 수는 200개로 제한하고, 넘으면 방 생성 요청을 오류로 돌려준다.
   - 방 생성 팝업의 기본값은 색상 수 4, 룰 "기본 룰" 이다. "피버 룰 (시작)" 의 잠금 여부는 오프라인과 같은 로컬 해금 기준(`isFeverStartRuleUnlocked()`)을 쓰며 서버는 검사하지 않는다.
   - 방 안의 인원 변화(참여자 입장·퇴장, 방장 이양)는 서버가 방 안의 두 사람에게 즉시 밀어 준다.
   - 참여자가 없는 동안 방장의 "시작" 버튼은 보이되 잠긴 상태로 두고 포커스와 실행에서 제외한다. (게임 시작 규칙 선택의 잠긴 항목과 같은 방식)

#### 5. 게임 진행

   - 게임 시작 결정은 서버가 한다. 방장의 시작 요청을 받은 서버는 방 `state` 를 `playing` 으로 바꾸고 양측에 `game_prepare` 를 보내 음영처리를 시작하게 한 뒤,
     3초 동안 이탈이 없으면 `game_start` 를 보낸다. 이탈이 있으면 `game_cancel` 을 보내 음영처리만 해제한다. 클라이언트가 자기 타이머로 시작을 결정하지 않는다.
   - 서버는 `game_start` 에 뿌요 지급 덱(512쌍 이상, 선택한 색상 수에 맞춘 색 목록)과 시작 시각을 담아 보낸다. 양측은 같은 덱을 같은 순서로 소비하며,
     덱이 모자라면 서버가 이어서 보낸다. 클라이언트가 자체 난수로 뿌요를 만들지 않는다.
   - 조작은 각 클라이언트가 자기 화면에 즉시 반영하고(입력을 서버 왕복만큼 늦추지 않는다), 같은 내용을 `{ "type": "input", "time", "kind", "value" }` 로 서버에 보낸다.
     서버는 이를 상대에게 그대로 중계한다. `kind` 는 리플레이의 `REPLAY_INPUT` 과 같은 이동·회전·빠른 하강·고정이고, `time` 은 `game_start` 기준 경과 밀리초다.
   - 연쇄 정산 결과(ATTACK, 싹쓸이, 피버 상태 변화)는 뿌요를 고정한 쪽이 서버에 보고하고 서버가 상대에게 전달한다.
     상쇄와 예고뿌요 낙하 순서는 기존 오프라인 계약(`resolveExplosions()` 경로)을 그대로 사용한다.
   - 패배 판정은 각 클라이언트가 자기 패배를 서버에 보고하고, 서버가 먼저 받은 쪽을 패자로 확정한다.
     두 보고의 게임 시각 차이가 50ms 이내이면 무승부로 처리하고 양측 WIN POINT 를 바꾸지 않는다.
   - WIN POINT 계산과 저장은 서버만 한다. 서버가 변경값과 갱신 후 값을 양측에 보내고, 클라이언트는 받은 값을 그대로 보여 준다.
   - 게임이 정상적으로 끝나면 결과 화면을 보여 준 뒤 양측 모두 자기 방 화면으로 돌아가고 방은 유지된다. (방 `state` 는 다시 `waiting` 이 된다.)
     그래서 온라인 대전 결과 화면에는 "다시 플레이" 를 두지 않고 "종료" 만 두며, 이 "종료" 는 메인 메뉴가 아니라 방 화면으로 돌아간다.
     다시 대전하려면 방장이 방 화면에서 "시작" 을 다시 누른다.
   - 온라인 대전은 리플레이를 기록하지 않는다. (설정의 `리플레이 사용` 과 무관하다. 서버 권위 덱과 입력 중계를 기록 형식에 담는 일은 이번 범위 밖이다.)
   - 진행도, GOLD, AI 학습, 가상 컨트롤러 관련 처리는 오프라인 "너랑 나랑" 과 같게 모두 제외한다.
     누적 승수 패널(`togetherWinCounts`)도 사용하지 않고, 중앙에는 양측 닉네임과 WIN POINT 를 보여 준다.
   - 탭을 바꾸거나 창을 가려도 게임은 계속 진행된다. (ESC 일시정지 불가와 같은 취지다. 브라우저가 프레임을 멈췄다 돌아오면 기존 `MAX_CATCH_UP_MS` 규칙대로 따라잡는다.)

#### 6. 화면과 입력

   - ESC 는 각 화면의 "취소"·"나가기" 와 같게 동작한다. 로그인 화면은 메인 메뉴로, 대기실은 로그아웃 후 메인 메뉴로, 방 화면은 대기실로 돌아가고,
     방 생성 팝업은 팝업만 닫는다. 게임 중 ESC 는 아무 동작도 하지 않는다. (위에 이미 명시)
   - 서버 응답을 기다리는 동안에는 해당 버튼을 다시 누를 수 없게 막는다. 요청이 실패하면 화면을 바꾸지 않고 메시지만 보여 준다.
   - 연결이 끊기면 메시지를 보여 준 뒤 온라인 플레이 로그인 화면으로 되돌린다.
   - 게임패드는 첫 번째로 연결된 것 하나만 1P 자리에 쓴다. (오프라인 "너랑 나랑" 의 0번·1번 고정 규칙은 적용하지 않는다.)


* 주의사항
- node.js 기반 서버와 python 기반 서버 각각 온라인 플레이 처리 기능을 따로 구현해야 함.
- 이 "온라인 플레이" 에서는 학습된 모델이나 학습 기능을 이용하지 않음.
- "온라인 플레이" 관련 백엔드 구현은 파일을 분리해 진행해야 함.
  node.js 기반 서버의 경우, 기존 nodeserver/server.js 는 수정을 최소화하고, "온라인 플레이" 관련 구현 대부분은 onlineplay.js 라는 파일을 만들어 이 안에 구현해야 함.
  python 기반 서버의 경우, 기존 python/pythonserver.py 는 수정을 최소화하고, "온라인 플레이" 관련 구현 대부분은 onlineplay.py 라는 파일을 만들어 이 안에 구현해야 함.
  온라인 플레이의 로그인 화면 및 대기실 등 UI 구현 또한 필요하므로 puyow.js 또한 수정되어야 함. 단, 기존 게임 룰과 모드들의 동작을 망가뜨리지 않아야 함.
- 기존의 "너랑 나랑" 플레이와는 달리 좌측 플레이어만 조작하므로, 조작키는 "너랑 나랑"이 아닌 기존 게임의 조작키와 동일하게 사용하면 됨.
   

----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 puyow.html, 
게임 핵심 코드는 puyow.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
INFO_FOR_AI.md 파일을 참고 후 작업해줘. 게임 룰 설명도 이 안에 기재되어 있어. 
(작업 후 다음 작업에 참고할 수 있도록 INFO_FOR_AI.md 파일을 업데이트해줘.)
puyow.js 를 수정한 경우, 작업 후 puyow.js 의 BUILDNO 를 1 증가시켜주고, package.json 의 version 의 패치 번호에 BUILDNO 값을 넣어줘. (이 버전값은 테스트는 하지 말아줘.)
주석 및 채팅창 답변은 모두 한국어로 해줘.
필요 시 git 히스토리를 읽는 건 되지만, 커밋하지는 말아줘.


----------------------------------------------------------
## 변경 사유

이미 적혀 있던 내용 중, 다른 내용과의 충돌 때문에 어쩔 수 없이 바꾼 부분과 그 이유다.

- **온라인 플레이 방 화면 상단의 "방 생성" 버튼을 뺐다.**
  원문에는 방 화면 상단에 "나가기", "방 생성" 버튼이 대기실 화면과 동일하게 있다고 적혀 있었다.
  그러나 "한 계정은 동시에 한 방에만 들어갈 수 있다"는 결정과 충돌하며(이미 방에 들어와 있는 상태에서 방을 또 만들 수 없다),
  보편적인 온라인 대전 게임의 방 화면에도 방 생성 버튼은 없다. 그래서 "나가기" 버튼과 닉네임 정보만 남기고 나머지 배치는 원문대로 대기실 화면과 같게 두었다.

- **온라인 대전 결과 화면에는 "다시 플레이" 를 두지 않기로 했다.**
  이 문서에 명시된 사항은 아니지만, 기존 오프라인 "너랑 나랑" 결과 화면 규칙(0번 버튼이 "다시 플레이")과는 달라지는 부분이라 함께 적어 둔다.
  온라인에서는 게임 시작 권한이 방장에게만 있고 시작 여부도 서버가 결정하므로, 양쪽이 각자 "다시 플레이" 를 누르는 방식이 성립하지 않는다.
  대신 결과 화면의 "종료" 로 방 화면에 돌아간 뒤 방장이 "시작" 을 다시 누르는, 보편적인 방 기반 대전 게임의 흐름을 따른다.



## 추가로 수행해야 할 작업

1. 온라인 플레이 지원 기능 사용여부 상수 추가 - node.js 백엔드, 파이썬 백엔드 서버 모두 해당
   둘 모두 상수를 두어, 온라인 플레이 지원 여부를 boolean 타입으로 켜고 끌 수 있게 만들어 주고 주석 꼭 달아줘.

2. SSL 기능 - node.js 백엔드, 파이썬 백엔드 서버 모두 해당
   별도의 상수를 두어, 필요한 SSL 인증서 파일들 각각 전체경로를 입력받아
   SSL 구현에 필요한 인증서 파일이 모두 존재하는 경우에 한하여, 해당 포트를 https 로 서비스하도록 변경해 줘.
   필요한 인증서 파일의 형식이 node.js 와 파이썬이 서로 호환이 안될테니, SSL 인증서 파일 형식을 일치시킬 수는 없을거야.
   추가로 SSL 활성화 + 온라인 플레이 지원 시, 웹소켓 통신 또한 SSL이 적용되어야 해.