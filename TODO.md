## 수정할 사항

1. 리더보드 구현

리더보드는 각 룰당 최고 달성 점수를 최대 10위까지 보여주는 기능이야.

우선 기록 규칙에 대해 설명할게.

리더보드 데이터는
로컬에만 저장 (localStorage 에 puyow_leaderboard 에 JSON 형식 저장) 할 거야.
너랑 나랑 게임 (오프라인, 온라인 모두) 과 구경 모드, 시뮬레이션에서는 기록하지 않아.
솔로몬과 대전 시에는 모드 상관없이 기록하지 않아.
(솔로몬만 제외일 뿐, onnx 모델을 사용하는 적과의 대전은 기록 대상에 포함이야.)
퍼즐뿌요의 경우 기록하지 않아.

리더보드 기록 시 경우의 수가 많다는 데 주의해야 해. 각 경우의 수마다 상위 10위를 따로 기록해야 해.
기본 룰, 피버 룰, 피버 룰 (시작) 모드의 경우 대전 모드이기 때문에, 각 색마다, 각 적마다 따로 10위가 기록되어야 하지.
대전 모드의 경우 사용자가 승리한 경우에만 기록 대상이 돼.

연습 모드와 연속 피버의 경우 색마다 따로 상위 10위를 기록하면 돼.
이 경우 승리 조건이 없으니 패배 시 최종 점수 기준으로 기록해.
단 패배 조건을 만족했을 때에만 기록하고, 그외 방법으로 게임을 빠져나간 경우는 기록하지 않아.

기록 당시의 플레이어 닉네임 (설정 화면에서 입력) 도 같이 기록해야 해. 플레이 시간은 기록하지 않아.
동점인 경우는 고려하지 않아. (조회 출력할 때에는 조회되는 순으로 10개만 출력)

2. 리더보드 조회 화면 구현

리더보드 조회는 leaderboard.html 에서 조회할 수 있도록 해.
스크립트는 puyow.js 에서 활용할 수 있는 것은 활용하되, 이 화면에서만 쓰는 스크립트는 puyow_leaderboard.js 에 넣어 줘.
화면 좌측에는 사이드바를 둬서 이 곳에 트리 구조의 메뉴 (하위 레벨 메뉴 개념이 존재하는) 를 두려고 해.
이 화면에서도 다크 모드 / 밝은 모드를 지원해야 하고 (사이드바 하단에 토글형 버튼 배치) 시스템 다크모드 여부를 탐지해 기본 설정해 줘,
룰 선택 후, 룰에 따라 색상/적 등 하위 선택지를 메뉴에서 선택하고 우측에서 점수 리스트 (닉네임도 같이 출력) 도 볼 수 있도록 구현해 줘.
이 화면에서도 다국어 처리가 필요해. 이 화면에서는 영어를 기본 언어로 하되 한국어, 일본어, 중국어, 독일어, 프랑스어를 지원해 줘.


----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 puyow.html, 
게임 핵심 코드는 puyow.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
INFO_FOR_AI.md 파일을 참고 후 작업해줘. 게임 룰 설명도 이 안에 기재되어 있어. 
(작업 후 다음 작업에 참고할 수 있도록 INFO_FOR_AI.md 파일을 업데이트해줘.)
puyow.js 를 수정한 경우, 작업 후 puyow.js 의 BUILDNO 를 1 증가시켜주고, package.json 의 version 의 패치 번호에 BUILDNO 값을 넣어줘. (이 버전값은 테스트는 하지 말아줘.)
주석 및 채팅창 답변은 모두 한국어로 해줘.
필요 시 git 히스토리를 읽는 건 되지만, 커밋하지는 말아줘.
