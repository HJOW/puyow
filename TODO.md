## 수정할 사항

1. lngui.py (GUI 기반 모델 학습 프로그램) 에 메뉴 도입

일반적인 GUI 인터페이스처럼 메뉴바를 도입한다.
일단 메뉴 그룹은 "File" 하나를 넣는다.

메뉴 그룹 "File" 안에는 "Save As..." 와 "Exit" 두 가지 메뉴 아이템을 넣는다.

2. 메뉴 아이템 "Exit"

이 메뉴 아이템은 항상 활성화된 상태여야 한다. (비활성화될 일이 없다는 뜻)
이 메뉴 클릭 시, 일단 이 프로그램 내 모든 버튼과 메뉴를 비활성화한 후 다음을 따른다.
    만약 학습 중이었거나 일시중지 상태라면, 학습 중단 예약을 한다. 중단됐다면 마지막 에피소드까지 학습된 내용을 모델 파일에 반영한다.
    (여기까지의 동작은 "Stop" 버튼 동작과 동일하다.)
    반영까지 끝냈다면 프로그램을 종료한다.
    학습 중이 아니었다면 즉시 프로그램을 종료하면 된다.

3. 메뉴 아이템 "Save As..."

학습 중이거나 일시중지 상태일 때에는 이 메뉴 아이템가 비활성화된 상태여야 한다.
이 메뉴 클릭 시, 
    먼저 Model output path 입력된 경로에 파일이 존재하는지 체크한다. 
    Model output path 입력된 경로에 파일이 존재하지 않으면 실패로 간주해 안내 메시지를 로그창에 출력하고 동작이 끝난다.
    파일이 존재하는 경우, 새 파일을 어디에 저장할 지를 묻기 위한, 파일 저장 대화상자를 연다. (Modal 로 동작해야 한다.)
    이 때, 확장자는 pt (현재의 PyTorch 모델 형식) 와 onnx (ONNX 호환 모델 형식) 둘 중에서 선택하게 한다.
    두 확장자 중 무엇을 선택하든, 파일 저장 대화상자에서 취소 혹은 그냥 닫은 경우 동작이 끝난다.
    두 확장자 중 무엇을 선택하든, 저장 위치를 지정해 저장을 선택한 경우, 파일 저장 대화상자를 닫고, "Start" 버튼을 비활성화한 뒤 다음을 따른다.
        pt 를 선택했던 경우, 현재의 모델을 그대로 다른 이름으로 저장을 수행한다. 저장이 끝나면 "Start" 버튼을 활성화한 후 동작이 끝난다.
        onnx 를 선택했던 경우, ONNX 호환 모델로 변환 후 해당 위치에 저장한다. 저장이 끝나면 "Start" 버튼을 활성화한 후 동작이 끝난다.
    ONNX 변환 작업 시, 진행 현황을 게이지바 ("Start" 버튼 아래에 있는 게이지바를 활용한다.) 로 표현한다.


* 주의사항

기존 모델의 호환성이 변경되지 않아야 하며, 게임 스크립트 (puyow.js) 도 변경되지 않아야 한다.
   
----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 puyow.html, 
게임 핵심 코드는 puyow.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
INFO_FOR_AI.md 파일을 참고 후 작업해줘. 게임 룰 설명도 이 안에 기재되어 있어. 
(작업 후 다음 작업에 참고할 수 있도록 INFO_FOR_AI.md 파일을 업데이트해줘.)
puyow.js 를 수정한 경우, 작업 후 puyow.js 의 BUILDNO 를 1 증가시켜주고, package.json 의 version 의 패치 번호에 BUILDNO 값을 넣어줘. (이 버전값은 테스트는 하지 말아줘.)
주석 및 채팅창 답변은 모두 한국어로 해줘.
필요 시 git 히스토리를 읽는 건 되지만, 커밋하지는 말아줘.