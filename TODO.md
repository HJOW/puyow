## 수정할 사항

1. 설정 화면에서 "사용 모델명" 값은 기본값으로 "gpt-5.6-luna" 로 입력되게 해줘.
   (이 모델명 기본값은 변수로 빼고 주석 달아줘.)
   설정을 localStorage 에서 불러온 이후에도 "사용 모델명" 이 공란이면 "gpt-5.6-luna" 을 기본값으로 입력해줘.

2. 설정 화면에 OpenAI API 테스트 기능을 구현해줘.
   사용 모델명 입력란과 "이 API키는 브라우저에만 저장됩니다." 사이에 버튼 "AI API 테스트" 버튼을 추가해줘.
   이 버튼은 기본적으로 비활성화되게 해줘.
   AI 서비스 제공자와 AI API 키, 사용 모델명을 모두 입력해야 이 버튼이 활성화되게 해줘.
   이 버튼이 활성화됐을 때는 다른 컴포넌트들처럼 방향키로 포커스 및 엔터 키로 클릭 및 마우스로 클릭이 가능해야 해.

   이 버튼을 클릭하면 먼저 AI 서비스 제공자와 AI API 키, 사용 모델명 입력 여부를 확인하고
   저장된 설정 데이터와 비교하여 변경되었는지도 확인해줘. 
   변경되었다면 "설정 저장 후 다시 시도해 주세요" 메시지를 화면에 띄우고 2초 뒤 페이드 아웃되어 사라지게 해줘. 
   (메시지 다국어 처리 해야 해.)
    
   API 관련 설정도 입력되어 있고 저장된 값과도 일치한다면,
   OpenAI 의 Responses API 를 사용해 테스트 요청을 날려줘. (백엔드 서버 따로 없어. 바로 OpenAI 쪽으로 직접 호출하게 구현해줘.)
   JSON 형식 데이터 생성 요청으로 아주 간단한 요청을 날려줘. 추론 성능은 low 로 해줘.
   API 문서는 https://developers.openai.com/api/docs/guides/structured-outputs 를 참고해줘.
   https://developers.openai.com/api/docs/guides/reasoning 또한 참고해줘.
   생성 결과물이 왔거나 오류가 났다면 화면 상에 메시지로 출력하고 2초 뒤 메시지가 페이드아웃되어 사라지게 해줘.

   그리고 생성 결과물이 온 경우 (테스트 성공한 경우)
   적 "솔로몬" 을 활성화 시켜줘. (적 "솔로몬"은 아래 2번에서 설명)
   이 적 "솔로몬" 은 AI API 테스트 후에만 활성화해줘. 
   매번 접속할 때마다 1회는 테스트를 해야 이 적 "솔로몬"을 이용할 수 있어야 해.
   활성화 안되어 있을 때는 적 "솔로몬" 을 적 선택 화면에서 숨겨서 보여주지 말아줘.

3. OpenAI API를 사용한 적 알고리즘 구현
   
   적 "솔로몬"을 만들어보자.
   솔로몬 클래스 또한 BundledEnemy 하위 타입으로 만들어야 해.

   표정 3개 (일반, 위기 상황, 우는 표정) 를 만들어야 하는데, "솔로몬" 이 원래 세계관에서 인간이었음에 착안하여 인간형으로 만들어줘.
   
   솔로몬의 알고리즘은 다음과 같아.
   컨트롤 타이밍이 될 때, 

   먼저 자신의 플레이 영역에서 X : 2, Y : 5 에 뿌요가 있거나 
   피버 룰이면서 자신의 플레이 영역에서 X : 2, Y : 6 또는 X : 3, Y : 5 에 뿌요가 있는 경우에는
   대체 인공지능으로 벨리알 Belial 의 인공지능과 동일하게 동작하게 해줘. 
   (API 응답 오기 전 패배로 끝나는 경우를 대비한 것으로, 나중에 이 대체 인공지능을 다른 적의 것으로 교체할 수 있게 변수로 빼줘.)
   
   위 "대체 인공지능" 조건에 만족하지 않으면
   설정값에 저장된 AI API 키와 모델명으로 OpenAI 의 Response API를 호출해 최적 위치를 JSON으로 응답해 달라고 요청해야 해.
   API 문서는 https://developers.openai.com/api/docs/guides/structured-outputs 를 참고해줘.
   https://developers.openai.com/api/docs/guides/reasoning 또한 참고해줘.
   추론 수준은 low 로 해줘.
   프롬프트 요청문을 통해 게임 규칙 (현재의 게임 모드에 대한) 과 현재 필드 상황, 제공받은 뿌요 갯수 모두를 포함해야 해.
   좌표계와 방향에 대한 설명도 해야 해.
   그리고 위 "대체 인공지능" 조건 또한 설명해서 해당 위치에는 두는 것을 삼가라고도 해야 해.
   응답 하는 JSON 스키마 정보도 넣어주고.
   (백엔드 서버 따로 없어. 바로 OpenAI 쪽으로 직접 호출하게 구현해줘.)
   타임아웃 시간은 6초로 두자.

   요청에 실패했거나 (단 타임아웃 초과의 경우는 제외) 응답받은 JSON 해석에 실패한 경우,
   게임 일시정지하고 화면 상단에 메시지를 띄워. 이 메시지는 2초 뒤 페이드아웃되어 사라지게 해줘.
   게임 재개 시 해당 컨트롤 타이밍은 위에서 설명한 "대체 인공지능" 으로 동작시켜.
   그 다음 컨트롤 타이밍에서는 다시 API 호출 시도해야 해.

   타임아웃 초과 시에는 즉시 "대체 인공지능" 을 사용해야 해.
   그 다음 컨트롤 타이밍에서는 다시 API 호출 시도해야 해.

   응답받는 데 성공했고, 뿌요를 어디에 둬야하고 회전해야 하는지까지 잘 받았다면 
   그 해당 위치 X좌표로 뿌요를 일단 이동시키고 회전 적용시켜. (아직 아래 방향키는 사용하지 말고.)
   다 됐다면 이제 게임 시작할 때 선택한 난이도에 따른 딜레이 후 아래 방향키로 뿌요를 내려.

   이와 같이 매 컨트롤 타이밍마다 API 호출하여 최적 위치와 회전여부를 받아 뿌요를 두게끔 솔로몬 적을 구현해줘.

   API 응답 데이터가 오기 전 뿌요가 바닥 혹은 다른 뿌요에 닿은 경우, 
   해당 요청은 취소해줘. 이미 뿌요가 닿았으니 중력 적용 타이밍으로 넘어가니 이번 차례에는 조작 못하는 거지.
   그래도 다음 번 컨트롤 타이밍이 되면 또 API 호출 시도해야 해.



----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 webpuyo.html, 
게임 핵심 코드는 webpuyo.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
예전 요청했던 내용은 OLD_PROMPTS.md 에 있어. 게임의 룰 설명도 OLD_PROMPTS.md 하단에 있어.