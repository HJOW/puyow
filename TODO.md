## 수정할 사항

본 게임 (puyow.js) 화면 크기 조절 알고리즘 변경

puyow.js 에 canvasFitMode 와 canvasFitMargin 변수를 마련했어.

canvasFitMode 가 0인 경우, 자바스크립트로 화면 크기를 조절하지 않고 HTML과 CSS로 맞추도록 할 거야.
(이 경우 게임 자체에서는 화면 방향 전환을 지원하지 않도록 해줘.)

canvasFitMode 가 1인 경우, 현재의 방식을 사용하지만 몇 가지는 보완이 필요해.

- canvasFitMargin 값을 읽어, canvas 와 웹 화면 간의 마진 공간을 주어야 해.
  (개발자가 이 마진 값을 이용해 빈 공간을 띄워 그 곳에 광고나 기타 컨텐츠를 HTML로 추가할 수 있도록 할 예정)
  canvasFitMargin 내부 원소들의 값은 px 단위로 사용해.

- canvas 크기를 화면의 가로, 세로 중 작은 쪽에 맞춰야 해.
  이를테면
  화면의 비율이 16:9보다 가로가 더 길어서 17:9 이라면, 세로 폭이 원래의 비율보다 더 짧으므로 캔버스 크기는 세로로 100% 차도록 (가로 방향은 양옆 공간이 남겠지) 크기를 조절해야 하고
  가령 화면의 비율이 16:9보다 가로가 더 짧아서 4:3 이라면, 이 때는 가로 폭이 원래의 비율보다 더 짧으므로 캔버스 크기는 가로로 100% 차도록 (세로 방향은 위아래 공간이 남겠지) 크기를 조절해야 해.
- 화면 비가 가로보다 세로가 더 길다면, 이건 화면 방향이 세로 (휴대폰 처럼) 상태인 것이지.
  이 경우에도 화면을 90도 꺾어서 봤을 때의 사용자 시점으로는 동일하게 동작해야 해.
  
이 작업 후에도 마우스 클릭이 정상동작하도록 클릭 좌표에도 신경 써야 해.
  
canvasFitMode 와 canvasFitMargin 값은 게임 초기화 전 변경이 가능하도록 함수 추가가 필요해.
이 함수에 대한 내용을 docs 디렉토리 내 Graphics.md 와 Graphics.en.md 문서에 추가해야 해.
또한 WebMCP 도구의 내용에도 반영이 필요해.

----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 puyow.html, 
게임 핵심 코드는 puyow.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
INFO_FOR_AI.md 파일을 참고 후 작업해줘. 게임 룰 설명도 이 안에 기재되어 있어. 
(작업 후 다음 작업에 참고할 수 있도록 INFO_FOR_AI.md 파일을 업데이트해줘.)
puyow.js 를 수정한 경우, 작업 후 puyow.js 의 BUILDNO 를 1 증가시켜주고, package.json 의 version 의 패치 번호에 BUILDNO 값을 넣어줘. (이 버전값은 테스트는 하지 말아줘.)
주석 및 채팅창 답변은 모두 한국어로 해줘.
필요 시 git 히스토리를 읽는 건 되지만, 커밋하지는 말아줘.
