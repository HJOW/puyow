## 수정할 사항

1. ort (ONNX Runtime for web) 존재여부 체크 (선택사항으로 적용)

게임 초기화 시 ort 이름의 변수로 ONNX 런타임 객체가 있는지를 체크해야 해.
대전 방식의 룰 (기본 룰, 피버 룰, 피버 룰 (시작) 및 구경 모드 소속 룰들) 의 적 선택 화면에서
적 "플라우로스" 및 그 이후의 적들은 ort 가 없으면 아예 보이지 않도록 숨겨줘.
(이미 이전 적 및 해당 적을 클리어한 전적이 있더라도.)

단, 갤러리 잠금해제는 이 부분하고는 관계없어. 해당 적을 클리어한 전적이 있으면 ort 가 없어도 갤러리에서는 잠금 해제되어야 해.
이 부분은 기존 hidden 이나 notAvail 을 활용하면 안되고 별도의 멤버변수로 관리해야 돼.

2. 적 "플라우로스" 구현

적 "플라우로스" 및 그 이후 적들 다 마찬가지로
ONNX 기반 모델을 이용할 거야.

일단 적 "플라우로스" 구현을 위해 src/onnx/model01.onnx 를 준비해놨어. (lngui.py 로 변환한 파일)
puyow.html 에도 ort.all.min.js, ort.webgl.min.js, ort.wasm.min.js 스크립트를 넣어놨어.
(더 필요한 라이브러리가 있으면, 작업 후 puyow.html 에 TODO로 명시해줘. npm 설치 대신 되도록이면 CDN을 사용할 거야.)

적 "플라우로스" 선택해 대전 시, 플라우로스의 플레이 알고리즘은 이 모델을 추론하여 동작하여야 해.
동작 자체는 기존 "솔로몬"의 경우처럼 동작해야 하지만
"솔로몬"과는 달리 ONNX 웹 런타임을 이용해 웹 스크립트 환경에서 추론이 동작해야 해. 
("솔로몬" 처럼 파이썬 백엔드를 거치는 방식은 안 돼.)
파이썬 스크립트로 구현된 코드 상당수를 자바스크립트로 가져와야 할 텐데,
그래도 보드 크기나 마진 레이트 등 puyow.js 의 상수나 변수에 이미 준비되어 있는 것은 같이 사용하는 쪽으로 가자.

모델을 역으로 학습하지는 않을거야.

플라우로스는 model01.onnx 를 사용하고
이후 적이 추가되면 모델 파일도 model02.onnx, model03.onnx 와 같은 이름으로 추가할 거야.
플라우로스 이후의 적들은 모델 파일과 1:1 대응될 예정이야.
그래서, 공통 구현을 위한 공통 클래스를 한번 거쳐서 구현을 하고 (공통 클래스는 새로 만들어야 돼.), 
플라우로스는 그 공통 클래스를 상속받는 식으로 구현해야 할 것 같아.
이후 적들도 ONNX 모델 사용 시 이 공통 클래스를 상속받도록.
다만 모델 파일 경로는 공통 클래스 내 멤버변수로 관리해야 해.

기본적으로 ONNX 추론 실패 시에는 앞 1수 시뮬레이션 함수를 호출하여 최적위치에 두는걸로 하자.
ort 가 없을 때는 어짜피 이 적을 선택 못하니 고려할 필요가 없지.
모델 로딩에 실패했다면 아예 게임을 시작하지 않고 안내문구 출력 후 적 선택 화면으로 돌아가는 게 나을 것 같아.
추론 및 출력 검증 단계에서 실패한 경우에만 앞 1수 시뮬레이션 함수를 호출하는거야.

ONNX 추론 도중에 게임이 멈추면 안 돼. CPU의 자연 낙하는 계속되어야 해. 다만 "빠르게 내리기" 만 지연시킬 수 있을 뿐.
만약 추론이 끝나기 전 조작뿌요가 땅이나 다른뿌요에 닿으면 그 턴은 어쩔 수 없이 넘어가야 해.

learning.py 및 lngui.py 로 모델 학습 시 이 "플라우로스" 및 이후의 적은 학습을 위한 상대 역할에 포함시키지 않을거야.
(정확히는, ONNX 추론 방식을 사용하는 적 모두를 학습 상대 역할로 사용하지 않을거야.)

추가로, 구경 모드에서도 제한 사항이 추가로 적용되어야 하는데
ONNX 추론 방식을 사용하는 적은 양쪽 중 한쪽에만 적용될 수 있어.
다른 상대는 반드시 ONNX 추론 방식을 사용하지 않는 적들 중에서만 선정되어야 해.

구현이 완료되면 적 "플라우로스"는 notAvail 을 false 로 해줘.

   
----------------------------------------------------------
## 참고사항

게임 플레이 페이지는 puyow.html, 
게임 핵심 코드는 puyow.js 에 구현하고 있어.
모든 파일은 UTF-8 인코딩을 사용하고, 기본 언어는 한국어야.
INFO_FOR_AI.md 파일을 참고 후 작업해줘. 게임 룰 설명도 이 안에 기재되어 있어. 
(작업 후 다음 작업에 참고할 수 있도록 INFO_FOR_AI.md 파일을 업데이트해줘.)
puyow.js 를 수정한 경우, 작업 후 puyow.js 의 BUILDNO 를 1 증가시켜주고, package.json 의 version 의 패치 번호에 BUILDNO 값을 넣어줘. (이 버전값은 테스트는 하지 말아줘.)
주석 및 채팅창 답변은 모두 한국어로 해줘.
필요 시 git 히스토리를 읽는 건 되지만, 커밋하지는 말아줘.